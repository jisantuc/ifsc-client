{
  "name": "ifsc-client",
  "requires": true,
  "dependencies": {},
  "devDependencies": {
    "xhr2": "^0.2.1"
  },
  "scripts": {
    "format": "find src test -iname '*.purs' | xargs purty format --write"
  }
}
